\documentclass[9pt]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[margin=0.57in,top=0.50in,bottom=0.52in]{geometry}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{multicol}
\usepackage{enumitem}
\usepackage{xspace}
\usepackage[table]{xcolor}
\usepackage{tikz}
\usepackage[hidelinks]{hyperref}
\usetikzlibrary{arrows.meta,positioning,fit,calc}

\definecolor{navy}{HTML}{17324D}
\definecolor{blue}{HTML}{2878B5}
\definecolor{cyan}{HTML}{DDEFF7}
\definecolor{orange}{HTML}{E47D36}
\definecolor{paleorange}{HTML}{FBE9DA}
\definecolor{green}{HTML}{237A57}
\definecolor{palegreen}{HTML}{E1F1E9}
\definecolor{ink}{HTML}{202A33}
\definecolor{muted}{HTML}{5B6873}

\hypersetup{colorlinks=true,linkcolor=navy,urlcolor=blue,citecolor=navy}
\setlength{\columnsep}{0.23in}
\setlength{\parindent}{0pt}
\setlength{\parskip}{2.1pt}
\setlist[itemize]{leftmargin=*,nosep,topsep=2pt}
\setlist[enumerate]{leftmargin=*,nosep,topsep=2pt}
\setlength{\abovecaptionskip}{3pt}
\setlength{\belowcaptionskip}{1pt}
\renewcommand{\arraystretch}{1.08}
\pagestyle{plain}

\newcommand{\project}{\textsc{CHIA-RDO}\xspace}
\newcommand{\metric}[1]{\textcolor{navy}{\textbf{#1}}}
\newcommand{\placeholder}[1]{\textcolor{orange}{\textbf{[#1]}}}
\newcommand{\papertitle}{From Adaptive $K$ to Routed RTL: A CHIA Loop for HEVC RDO Co-Design}
\newcommand{\sectionrule}{\vspace{-2pt}\textcolor{blue}{\rule{\linewidth}{0.6pt}}\vspace{1pt}}
\newcommand{\tightsection}[1]{\vspace{3pt}{\large\bfseries\color{navy}#1}\par\sectionrule}
\newcommand{\tightsubsection}[1]{\vspace{2pt}{\bfseries\color{ink}#1}\par}
\newcommand{\evidence}[1]{\textcolor{muted}{\footnotesize #1}}

\tikzset{
  block/.style={draw=navy,thick,rounded corners=2pt,fill=cyan,
                align=center,minimum height=6.5mm,inner xsep=4pt,font=\scriptsize},
  hw/.style={block,fill=paleorange,draw=orange},
  result/.style={block,fill=palegreen,draw=green},
  flow/.style={-{Latex[length=2mm]},thick,draw=navy},
  feedback/.style={-{Latex[length=2mm]},thick,dashed,draw=orange}
}

\begin{document}
\color{ink}

% PAGE 1: problem, proposal, and software policy.
\begin{center}
  {\fontsize{18}{20}\selectfont\bfseries\color{navy}\papertitle\par}
  \vspace{4pt}
  {\small Nguyen Hieu Duong $\cdot$ Duy Hieu Bui $\cdot$
  Vietnam National University $\cdot$
  \href{mailto:24020497@vnu.edu.vn}{24020497@vnu.edu.vn}\par}
  \vspace{2pt}
  {\footnotesize Artifact: \url{https://github.com/duongnguyenhieu/CHIA-RDO}, release \texttt{chia-rdo-v1.0.0}}
\end{center}
\vspace{-3pt}

\begin{multicols}{2}
\tightsection{Abstract}
HEVC intra coding exposes 35 luma prediction modes, making full
rate--distortion optimization (RDO) expensive.  Yet candidate count alone is
not a hardware objective: an FPGA with $P$ lanes executes
$\lceil K/P\rceil$ batches, while replication changes area, routing, timing,
and power.  We propose \project, an evidence-preserving CHIA loop that joins
pinned HM-16.20 experiments, a history-aware proposal controller, analytical
screening, bit-exact Verilator regression, Pareto updates, GCP execution, and
Vivado implementation.  A frozen Adaptive-$K$ policy reduces held-out RDO
evaluations by \metric{38.21\%} while retaining \metric{97.91\%} of Full-RDO
winners; its aggregate Y-PSNR BD-rate is $-0.026\%$, interpreted as no observed
penalty on this limited corpus.  Physical feedback first exposed a 170-level
combinational baseline and then an 80-level serial winner scan.  Resource
sharing, a depth-3 quant/dequant split, corpus-qualified 31-bit cost, and a
balanced reduction tree yield a routed 16-lane kernel at a derived
\metric{68.120 MHz} and \metric{8.134 M candidates/s}, 2.128$\times$ the P1
baseline proxy.  The design does \emph{not} close 200 MHz, and kernel rate is
not encoder throughput.

\tightsection{1. Problem and Opportunity}
Stock HM ranks all 35 modes by rough mode decision (RMD), keeps a PU-size
dependent shortlist, inserts most-probable modes, and performs expensive RDO.
Prior work shows that content-adaptive pruning can reduce this work
\cite{gwon2019}.  The cross-layer problem is that two policies with equal mean
$K$ may have different hardware cost because latency is quantized by
$\lceil K/P\rceil$.  Conversely, adding lanes may reduce batches but fail
placement or deepen a routing-critical cone.

\project therefore optimizes a contract spanning three evidence classes:
software coding quality, cycle-accurate RTL correctness, and routed physical
results.  Analytical estimates are retained but never relabeled as measured
hardware.

\tightsubsection{Contributions}
\begin{enumerate}
  \item A composable CHIA graph connecting policy search, matched HM curves,
        RTL checks, cloud workers, persistent history, and backend feedback.
  \item A frozen, independently specified Adaptive-$K$ policy, evaluated
        separately from later hardware-aware policies.
  \item A physically driven RTL evolution from monolithic replication to
        serial shared lanes and balanced winner reduction.
  \item Machine-readable identities, hashes, cache behavior, and Pareto
        history supporting replay and claim auditing.
\end{enumerate}

\tightsection{2. Adaptive-$K$ Contract}
For mode $m$, HM instrumentation forms the stable ranking
\begin{equation}
  C_{\mathrm{rough}}[m]=\mathrm{SATD}[m]
      +\mathrm{bits}[m]\sqrt{\lambda}.
\end{equation}
Our Full-RDO anchor evaluates all 35 ranked modes.  The frozen
\texttt{adaptive-threshold.v1} normalizes the minimum rough cost by luma block
area and sample range, then chooses
\begin{equation}
K=\begin{cases}
4,&\widehat C_{\min}\le 0.04,\\
16,&0.04<\widehat C_{\min}\le 0.20,\\
35,&\widehat C_{\min}>0.20.
\end{cases}
\end{equation}
The controller does not consume $P$, scheduler state, QP, or activity.  This
separation prevents hardware exploration from silently changing the selected
software policy.  Relative-SATD and hardware-aware variants receive distinct
experiment versions rather than being folded into the frozen controller.

Selection used two deterministic synthetic workloads.  Evaluation then ran
without retuning on three predeclared held-out synthetic families.  All matched
curves use HM-16.20 commit
\texttt{22178e370178133438c0339f57b3b3a29f112909}, Main 8-bit 4:2:0
All-Intra, four frames, and QP 22/27/32/37.
\end{multicols}

\clearpage
% PAGE 2: complete loop and checked RTL architecture.
\tightsection{3. End-to-End Co-Design Architecture}
\begin{center}
\begin{tikzpicture}[node distance=5mm and 5mm]
  \node[block,text width=19mm] (proposal) {History-aware\\proposal};
  \node[block,text width=18mm,right=of proposal] (screen) {Analytical\\screen};
  \node[block,text width=18mm,right=of screen] (dispatch) {CHIA/Ray\\dispatch};
  \node[block,text width=18mm,right=of dispatch] (hm) {Pinned HM\\quality curves};
  \node[hw,text width=18mm,right=of hm] (rtl) {Verilator\\RTL checks};
  \node[hw,text width=18mm,right=of rtl] (vivado) {Vivado\\route};
  \node[result,text width=18mm,right=of vivado] (persist) {Schema + atomic\\persistence};
  \foreach \a/\b in {proposal/screen,screen/dispatch,dispatch/hm,hm/rtl,rtl/vivado,vivado/persist}
    \draw[flow] (\a) -- (\b);
  \draw[feedback] (persist.south) -- ++(0,-5mm) -| node[pos=.48,below,font=\scriptsize,
    text=orange]{Pareto update, cache, failed-point history} (proposal.south);
  \node[draw=muted,dashed,rounded corners,fit=(proposal)(screen)(dispatch),inner sep=3pt,
        label={[font=\scriptsize\color{muted}]above:decision and orchestration}] {};
  \node[draw=orange,dashed,rounded corners,fit=(hm)(rtl)(vivado),inner sep=3pt,
        label={[font=\scriptsize\color{orange}]above:evidence-producing tools}] {};
\end{tikzpicture}
\end{center}
\vspace{-3pt}
\begin{center}
\footnotesize\textbf{Figure 1.} Complete \project loop. Dominated and failed
points remain in history, preventing repeated proposals and turning backend
failures into the next architecture decision.
\end{center}

\begin{multicols}{2}
Native \texttt{ChiaFunction} nodes pass Ray object references asynchronously.
The deterministic controller consumes completed records, excludes existing
experiment IDs, and rewards novelty and Pareto-boundary exploration.  The
initial graph screened 72 $(P,\text{depth},\text{buffer},\text{width})$ points.
A multi-wave campaign accumulated 114 hardware/history records, 107 successful
logical experiments, 11 matched HM curves (132 encodes), and a 10-point Pareto
set.  The calibrated graph later screened 832 combinations over $P=1\ldots16$,
depth 2/3, and cost width 31--56.  It produced 16 Pareto points; deliberate
replay returned 832/832 cache hits with no new evaluation.

\tightsection{4. RTL Architecture, Checked Against Source}
The final top level latches an event containing $K\le35$ ranked mode
descriptors, QP, Q16.16 $\lambda$, references, and a 4$\times$4 original luma
block.  A ready/valid scheduler launches candidate $bP+l$ to lane $l$ and masks
the partial final batch.  Up to 16 lanes operate concurrently; each lane is
internally serial and accepts another candidate only after retirement.

\begin{center}
\begin{tikzpicture}[node distance=3.0mm]
  \node[block,text width=25mm] (ctrl) {Frozen Adaptive-$K$\\$K\in\{4,16,35\}$};
  \node[block,text width=25mm,below=of ctrl] (buf) {Ranked descriptor buffer\\mode, rank, rate bits};
  \node[block,text width=25mm,below=of buf] (sched) {$P$-way batch scheduler\\$bP+l<K$};
  \node[hw,text width=25mm,below=of sched] (lanes) {Serial lane $\times P$\\$P\le16$};
  \node[result,text width=25mm,below=of lanes] (tree) {Balanced min tree\\cost, then stable rank};
  \draw[flow] (ctrl)--(buf); \draw[flow] (buf)--(sched);
  \draw[flow] (sched)--(lanes); \draw[flow] (lanes)--(tree);

  \node[hw,text width=16mm,right=7mm of lanes,yshift=18mm] (pred) {Prediction\\+ residual};
  \node[hw,text width=15mm,right=of pred] (fwd) {Forward DST\\pass 1/2};
  \node[hw,text width=15mm,right=of fwd] (quant) {Quantize};
  \node[hw,text width=15mm,right=of quant] (deq) {Dequantize};
  \node[hw,text width=15mm,right=of deq] (inv) {Inverse DST\\pass 1/2};
  \node[hw,text width=16mm,right=of inv] (sse) {Reconstruct\\+ SSE};
  \node[hw,text width=15mm,right=of sse] (rd) {$D+\lambda R$\\RD cost};
  \foreach \a/\b in {pred/fwd,fwd/quant,quant/deq,deq/inv,inv/sse,sse/rd}
    \draw[flow] (\a)--(\b);
  \draw[flow] (lanes.east) -- ++(5mm,0) |- (pred.west);
  \draw[flow] (rd.south) -- ++(0,-12mm) -| (tree.east);
  \node[draw=orange,dashed,rounded corners,fit=(pred)(fwd)(quant)(deq)(inv)(sse)(rd),
        inner sep=3pt,label={[font=\scriptsize\color{orange}]above:one 132-cycle depth-3 candidate lane}] {};
\end{tikzpicture}
\end{center}
\vspace{-4pt}
\begin{center}
\footnotesize\textbf{Figure 2.} Implemented RTL hierarchy and lane FSM.  Exact
HM candidate rate is external; hardware CABAC/context evolution is out of scope.
\end{center}

The lane implements planar, DC, and angular modes 0--34; 4$\times$4 8-bit
prediction/residual; two-pass HEVC integer DST; scalar quantization with RDOQ,
transform skip, sign hiding, and scaling lists disabled; inverse transform and
clipped reconstruction; SSE; and fixed-point RD cost.  Depth 3 separates
quantization from dequantization.  One candidate takes 132 cycles and a batch
retires in 134 cycles without backpressure.

The reduction compares valid pairs lexicographically: lower RD cost wins and
an exact tie selects lower stable rank.  In the P16 revision, a $\log_2P$
balanced tree replaces the P-deep scan.  The 31-bit cost is safe for the
observed maximum 1,312,532,898 only; it is not an arbitrary-input proof.

\tightsubsection{Verification evidence}
Final campaigns executed 121,600 vector/candidate checks.  The depth-3 serial
corpus alone contains 8,960 vectors covering all 35 modes and four QPs; the
tree campaign adds 10,560 P1/P8/P16 evaluations including backpressure.  Stage
values, order, winners, cycle counts, dropped/duplicate checks, and interfaces
all passed with zero failures.
\end{multicols}

\clearpage
% PAGE 3: software evaluation and physical feedback.
\tightsection{5. Coding and Hardware Evaluation}
\tightsubsection{Coding trade-off}
\begin{center}
\scriptsize
\setlength{\tabcolsep}{3.1pt}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{0.21\textwidth}
  >{\raggedright\arraybackslash}X r r r r@{}}
\toprule
\textbf{Method / corpus} & \textbf{Candidate rule} & \textbf{Avg. $K$} &
\textbf{RDO red.} & \textbf{Retention} & \textbf{BD-rate} \\
\midrule
Full-RDO anchor & All 35 rough-ranked modes & 35.000 & 0.00\% & 100.00\% & 0.000\% \\
Fixed-$K$8 / discovery & First 8 ranked modes & 8.000 & 77.14\% & 68.05\% & +3.574\% \\
Adaptive-v0 / discovery & Confidence gap; $K=4/8/16$ & 11.568 & 66.95\% & 75.67\% & +2.866\% \\
Relative-SATD / discovery & Threshold count, quantized $K$ & 25.624 & 26.79\% & 96.07\% & +0.528\% \\
Frozen threshold / discovery & Normalized rough cost; $K=4/16/35$ & 18.651 & 46.71\% & 90.25\% & +0.755\% \\
\rowcolor{palegreen}
Frozen threshold / held-out & Same policy, no retuning & 21.628 & \textbf{38.21\%} & \textbf{97.91\%} & $-$0.026\% \\
HW-aware Track-B / P8 & Joint quality/cycle objective & 16.456 & 52.98\% & 88.02\% & $-$0.054\% \\
\bottomrule
\end{tabularx}
\end{center}
\vspace{-2pt}
\begin{center}
\footnotesize\textbf{Table 1.} Project results relative to the Full-RDO anchor.
The held-out set contains three synthetic families; negative aggregate BD-rate
is not claimed as general quality improvement.
\end{center}

\begin{multicols}{2}
The frozen policy responds to content.  It removes 13.95\% and 13.30\% of RDO
work on mixed-texture and repetitive families, but 87.36\% on smooth/edge
content.  Worst individual-run winner retention is 91.00\%.  On the held-out
aggregate, Fixed-$K$16 removes more evaluations (54.29\%) but retains only
85.70\% of Full-RDO winners.  Track-B is a separately versioned, more
aggressive hardware-oriented point; it is not evidence that the frozen policy
was altered after selection.

The Gwon--Choi relative-SATD Bayesian method reports 31.54\% average encoding
time reduction and +0.93\% BD-rate against HM-16.6 under its own Intra Main
condition \cite{gwon2019}.  It starts from stock-HM RMD modes and includes a
minimum-risk Bayesian classifier.  Our relative-SATD baseline instead starts
from all 35 modes and uses threshold counting only, so the published result is
context rather than a direct comparison.

\tightsection{6. Physical Feedback as an Agent Input}
The first P-way baseline replicated a monolithic candidate datapath.  At P1,
the critical path traversed prediction, residual, both forward-transform
passes, quant/dequant, both inverse passes, reconstruction, and distortion:
170 logic levels, including 69 CARRY8 levels.  P2 reached 175 levels, and P4
exceeded LUT/CARRY capacity before placement.

The next proposal serialized work within each lane and inserted state
boundaries between major arithmetic phases.  This raised Fmax and reduced
per-lane area, allowing P16 to route, but a serial 16-way winner scan became an
80-level path.  The final balanced tree reduced that path; the routed critical
path is now 39 levels inside forward-transform accumulation and ends at a
residual-sample RAM input.

\tightsubsection{Measured interpretation}
For a failed 5 ns timing constraint, we report
\begin{equation}
 f_{\mathrm{derived}}=\frac{1}{5\,\mathrm{ns}-\mathrm{WNS}},\qquad
 T_{\mathrm{kernel}}=\frac{P f_{\mathrm{derived}}}{C_{\mathrm{batch}}}.
\end{equation}
This is a post-route comparison proxy.  It is neither timing closure nor
encoder frames/s.  The selected P16 design routes with WNS $=-9.680$ ns, hence
$f_{\mathrm{derived}}=68.120$ MHz and
$T_{\mathrm{kernel}}=8.134$ M candidates/s for 134-cycle batches.

\tightsubsection{Resource budget}
On the KV260 device (117,120 LUTs, 1,248 DSPs), the final design uses 52.26\%
of LUTs and 65.38\% of DSPs.  Against the project's 70\%-of-device integration
budget, this is 74.66\% of the LUT allowance and 93.47\% of the DSP allowance,
leaving 57 DSPs.  P16 is the throughput endpoint of the Pareto frontier, not
the minimum-area or minimum-power point.
\end{multicols}

\clearpage
% PAGE 4: routed comparison, reproducibility, limitations, and references.
\tightsection{7. Routed Microarchitecture Comparison}
\begin{center}
\scriptsize
\setlength{\tabcolsep}{2.8pt}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}X r
  >{\raggedright\arraybackslash}p{0.12\textwidth} r r r r r@{}}
\toprule
\textbf{Architecture} & \textbf{$P$} & \textbf{Result} & \textbf{LUT} &
\textbf{DSP} & \textbf{WNS} & \textbf{Derived Fmax} & \textbf{Kernel rate} \\
\midrule
Monolithic combinational & 1 & Routed & 36,454 & 178 & $-$60.395 ns & 15.292 MHz & 3.823 M/s \\
Monolithic combinational & 2 & Routed & 70,817 & 356 & $-$64.653 ns & 14.357 MHz & 7.178 M/s \\
Monolithic combinational & 4 & Placement failed & 140,102$^\dagger$ & 712 & N/A & N/A & N/A \\
Shared serial, d3, w31 & 8 & Routed & 30,813 & 408 & $-$9.620 ns & 68.399 MHz & 4.084 M/s \\
Shared serial, P16 scan & 16 & Routed & 60,232 & 816 & $-$19.092 ns & 41.508 MHz & 4.956 M/s \\
\rowcolor{palegreen}
Shared serial, P16 tree & 16 & Routed & \textbf{61,207} & \textbf{816} &
\textbf{$-$9.680 ns} & \textbf{68.120 MHz} & \textbf{8.134 M/s} \\
\bottomrule
\end{tabularx}
\end{center}
\vspace{-2pt}
\begin{center}
\footnotesize\textbf{Table 2.} Vivado 2024.2, \texttt{xck26-sfvc784-2LV-c},
5 ns out-of-context constraint. $^\dagger$Synthesized LUT count; placement did
not run. No failed value is interpolated.
\end{center}

\begin{multicols}{2}
The balanced tree costs 975 LUTs and 45 FFs over the P16 serial scan, leaves
DSP count unchanged, and reduces estimated power from 2.338 W to 2.288 W.  It
improves derived Fmax by 64.1\% and kernel rate by 64.1\%.  Relative to the
monolithic P1 proxy, its candidate rate is 2.128$\times$; relative to P2 it is
1.133$\times$.  The calibrated CHIA model predicts P16 within 0.14\% for LUT,
0.45\% for Fmax/throughput, and 0.43\% for power.

\tightsection{8. Reproducibility and Deliverables}
Local/GCP parity produced identical bitstream, reconstruction, and trace hashes
for a reference encode.  GCP workers later replayed RTL campaigns with zero
failures.  Post-run Compute Engine queries found no remaining instances,
disks, or reserved addresses.  Actual billed spend and promotional-credit
balance remain unavailable and are not replaced by estimates.

The artifact preserves policy versions, manifests, source and corpus hashes,
raw Vivado reports, machine-readable result schemas, and Pareto history.
Principal deliverables are:
\begin{itemize}
  \item CHIA proposal/evaluation graphs and deterministic policy controller;
  \item pinned HM patches, matched quality curves, and synthetic fixtures;
  \item parameterized SystemVerilog, golden vectors, and Verilator campaigns;
  \item analytical replay, model calibration, routed reports, and parsers.
\end{itemize}

\tightsection{9. Limitations and Next Step}
The coding study uses small deterministic synthetic All-Intra workloads, not
standard natural-video classes.  RTL is limited to 4$\times$4 8-bit luma and
excludes chroma, inter prediction, larger transforms, RDOQ, syntax coding, and
hardware CABAC/context evolution.  Exact HM candidate bit counts are supplied
externally.  The 31-bit cost width is corpus-qualified, not formally bounded
for arbitrary input.

All physical runs are out of context and omit board-level clocks, pins, AXI,
and encoder integration.  Most importantly, the selected implementation has
negative WNS and does not close 200 MHz.  The next loop should pipeline or
restructure forward-transform accumulation, now the measured critical path,
then repeat regression and route before any board-frequency claim.

Within these limits, \project demonstrates the intended agentic co-design
behavior: it preserves a software-quality contract, searches reproducibly,
checks a bit-exact kernel, and uses failed backend evidence to produce a
specific microarchitectural revision rather than merely requesting more
parallelism.

\footnotesize
\begin{thebibliography}{5}
\setlength{\itemsep}{1pt}
\bibitem{chia2026}
CHIA, ``An Open Framework for Agile and Principled Hardware/Software Co-Design
Research,'' arXiv:2606.27350, 2026.

\bibitem{gwon2019}
D. Gwon and H. Choi, ``Relative SATD-based Minimum Risk Bayesian Framework for
Fast Intra Decision of HEVC,'' \emph{KSII Trans. Internet Inf. Syst.}, vol. 13,
no. 1, pp. 385--405, Jan. 2019, doi:10.3837/tiis.2019.01.022.

\bibitem{hm1620}
ITU-T/ISO/IEC, ``High Efficiency Video Coding Test Model HM-16.20.''

\bibitem{artifact}
\project artifact, \url{https://github.com/duongnguyenhieu/CHIA-RDO}, release
\texttt{chia-rdo-v1.0.0}.
\end{thebibliography}

\tightsubsection{AI Assistance Acknowledgement}
\evidence{The authors used OpenAI OpenCode to assist with code review, artifact
organization, and manuscript editing. The human authors reviewed the output and
are responsible for the paper's contents, tone, and quality.}
\end{multicols}

\end{document}
